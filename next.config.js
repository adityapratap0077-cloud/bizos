/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special config needed. All secrets stay in environment variables;
  // the Supabase service-role key is never used by this app.
};

module.exports = nextConfig;
